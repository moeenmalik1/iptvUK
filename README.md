# UK LTD IPTV

A minimal Next.js codebase using:

- App Router (`app/` directory)
- JavaScript (no TypeScript)
- Tailwind CSS

## Getting started

```bash
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000).
